---
# Secrets must NEVER be committed to Git in plain text.
# This manifest is a TEMPLATE. Before applying:
#   1. Fill in the base64-encoded values below.
#   2. Or use a secrets manager (Sealed Secrets, External Secrets Operator, Vault).
#
# To encode a value: echo -n 'your-secret' | base64
apiVersion: v1
kind: Secret
metadata:
  name: taskguard-secrets
  namespace: taskguard
  labels:
    app.kubernetes.io/name: taskguard
    app.kubernetes.io/component: secrets
type: Opaque
data:
  # Placeholder — replace with real base64-encoded secrets before deployment.
  # Example: echo -n 'super-secret-key' | base64
  APP_SECRET: ""
